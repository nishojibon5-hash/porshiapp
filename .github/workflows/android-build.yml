name: Build Porshi Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Capacitor এর নতুন চাহিদামত ২২ সেট করা হলো

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Dependencies
        run: |
          npm install --legacy-peer-deps
          npm run build --if-present

      - name: Capacitor Sync
        run: |
          npx cap sync android

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug -x lint -x test

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: porshi-apk-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error # ফাইল না থাকলে এবার এরর দিবে, আপনাকে আর ধোঁকায় ফেলবে না
